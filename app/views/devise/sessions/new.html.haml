.row
  .col-md-4

  .col-md-4
    = simple_form_for(resource, :as => resource_name, :url => session_path(resource_name), :html => {:class => 'panel panel-default'}) do |f|
      .panel-heading 
        %h4 Inicia sesión

      .panel-body
        .form-group
          = f.input :email, :required => false, :autofocus => true, :input_html => {class: 'form-control'}, label: false, placeholder: 'Correo', placeholder: 'Correo electrónico'
        .form-group
          = f.input :password, :required => false, :input_html => {class: 'form-control'}, label: false, placeholder: 'Contraseña'

        .form-group
          = f.input :remember_me, :as => :boolean if devise_mapping.rememberable?
        
        %hr
        .form-group
          = f.button :submit, "Iniciar sesión", class: 'btn-block btn-primary btn-md'

  .col-md-4